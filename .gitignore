# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-standalone
dist-ssr
release
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript build info
*.tsbuildinfo

# OS
Thumbs.db
ehthumbs.db
Desktop.ini

# Personal data — no real CSV in repo
sample.csv
zen_*.csv
*.dump.json

# Claude Code local-only settings
.claude/settings.local.json

# AI-agent tooling (skills, lockfiles) — developer-local only,
# not part of the project source. `.claude/launch.json` is kept
# tracked because it's a project-level run config (akin to
# `.vscode/launch.json`).
.agents/
.claude/skills/
skills-lock.json

